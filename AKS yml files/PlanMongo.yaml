apiVersion: apps/v1
kind: Deployment
metadata:
  name: planmongo-deployment
  labels:
    app: plan-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plan-mongodb
  template:
    metadata:
      labels:
        app: plan-mongodb
    spec:
      containers:
        - name: plan-mongodb
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-username                  
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: planmongo-service
spec:
  selector:
    app: plan-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27024