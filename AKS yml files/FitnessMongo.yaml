apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitnessmongo-deployment
  labels:
    app: fitness-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fitness-mongodb
  template:
    metadata:
      labels:
        app: fitness-mongodb
    spec:
      containers:
        - name: fitness-mongodb
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-username                  
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: fitnessmongo-service
spec:
  selector:
    app: fitness-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27021