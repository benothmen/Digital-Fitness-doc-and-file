apiVersion: apps/v1
kind: Deployment
metadata:
  name: profileapi-deployment
  labels:
    app: profileapi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: profileapi
  template:
    metadata:
      labels:
        app: profileapi
    spec:
      containers:
        - name: profileapi
          image: fitnessacr.azurecr.io/profileapi:v2
          imagePullPolicy: IfNotPresent          
          ports:
            - containerPort: 80
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: DatabaseSettings__ConnectionString
              valueFrom:
                  configMapKeyRef:
                    name: mongo-configmap
                    key: connection_string
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.1"
            limits:
              memory: "128Mi"
              cpu: "0.3"
      imagePullSecrets:
        - name: acr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: profileapi-service
spec:
  selector:
    app: profileapi
  ports:
    - protocol: TCP
      port: 80           