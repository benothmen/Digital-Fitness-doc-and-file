apiVersion: apps/v1
kind: Deployment
metadata:
  name: inquirymongo-deployment
  labels:
    app: inquiry-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inquiry-mongodb
  template:
    metadata:
      labels:
        app: inquiry-mongodb
    spec:
      containers:
        - name: inquiry-mongodb
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-username                  
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: inquirymongo-service
spec:
  selector:
    app: inquiry-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27022