apiVersion: apps/v1
kind: Deployment
metadata:
  name: profilemongo-deployment
  labels:
    app: profile-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-mongodb
  template:
    metadata:
      labels:
        app: profile-mongodb
    spec:
      containers:
        - name: profile-mongodb
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-username                  
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: profilemongo-service
spec:
  selector:
    app: profile-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27025