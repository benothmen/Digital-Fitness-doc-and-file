apiVersion: apps/v1
kind: Deployment
metadata:
  name: notificationmongo-deployment
  labels:
    app: notification-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-mongodb
  template:
    metadata:
      labels:
        app: notification-mongodb
    spec:
      containers:
        - name: notification-mongodb
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-username                  
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: notificationmongo-service
spec:
  selector:
    app: notification-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27023