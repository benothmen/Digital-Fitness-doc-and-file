apiVersion: apps/v1
kind: Deployment
metadata:
  name: dietmongo-deployment
  labels:
    app: diet-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diet-mongodb
  template:
    metadata:
      labels:
        app: diet-mongodb
    spec:
      containers:
        - name: diet-mongodb
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-username                  
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: mongo-root-password
---
apiVersion: v1
kind: Service
metadata:
  name: dietmongo-service
spec:
  selector:
    app: diet-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27020